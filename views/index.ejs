<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
    <title>Document</title>
</head>
<body>
    <div class="container">

   
    <h1>Url Shortner</h1>
    <form action="/shortUrls" method="POST" class="my-4 form-inline">
        <label for="fullURL" class="sr-only">URL</label>
        <input type="url" name="fullURL" id="fullURL" placeholder="Type URL" required class="form-control col mr-2 mb-2">
        <button class="btn btn-success" type="submit">Shrink</button>
    </form>

    <table class="table table-striped table-responsive">
        <thead>
            <tr>
                
                <th>Full Url</th>
                <th>Short Url</th>
                <th>Clicks </th>
            
            </tr>

        </thead>
        <tbody>
            <% shortUrls.forEach((shortUrl)=>{%>
                <tr>
                    <td><a href="<%=shortUrl.fullUrl%>"><%=shortUrl.fullUrl%></a></td>
                    <td><a href="<%=shortUrl.shortUrl%>"><%=shortUrl.shortUrl%></a></td>
                    <td><%=shortUrl.shortUrl%></td>
    
                </tr>
    <%         })%>
            
        </tbody>
    </table>
</div>
</body>
</html>